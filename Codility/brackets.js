// Determine whether a gived string of parentheses (multiple types) is properly nested.

function solution(S) {
  if (S.length % 2 === 1 || S.length === 1) return 0;
  const nested = [];
  for (let i = 0; i< S.length; i++){
      if (S[i]==="{" )
  }
}

// Done
